// 08/08

// Lista sequencial (acho que pode ser chamado de ordenadas também)
    //Lineares
    //Listas sequenciais são aquelas que a ordem se mantém para o usuário
    //Utiliza padrões Parametros/Chaves


    //Comandos como Define trocam valores durante a compilação "Tipo um ctrl F"


    //Mesmo elemento da mempria é o observado pelo usuário
    //Sentinela (Adicionar um novo valor no final)
    //Complexidade O(n)
        //Propocional ao N
        //Busca O(N) (Normalmente a mais usada, tem que ser bem otimizada)
        //Inserção O(N)
        //Exclusão O(N)
    //Bubble Sort por exemplo é O(N)²
    //

